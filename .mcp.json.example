{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": [
        "-y",
        "@supabase/mcp-server-supabase",
        "--access-token",
        "YOUR_SUPABASE_PERSONAL_ACCESS_TOKEN",
        "--project-ref", 
        "YOUR_PROJECT_REF",
        "--read-only"
      ]
    }
  }
}
